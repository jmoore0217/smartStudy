# smartStudy

Install Python 3.10 or later.

Save the file as smartstudy.py.

Open a terminal in the file directory.

Run the command:
python smartstudy.py

Enter assignment details when prompted to generate a schedule.
